#!/usr/bin/env zsh

# ~/.zshenv
# Place pure environment variables here if you want them to apply even when
# Zsh is used in non-interactive contexts. For now this is intentionally empty
# to keep the portable configuration minimal.


